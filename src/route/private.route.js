import express from "express";
// import { HelloController } from "../controller/hello.controller.js";

const privateRouter = express.Router();

// privateRouter.get("/", HelloController.sayHello);

export { privateRouter };
